# AgriCheck V3 Frontend
VITE_APP_NAME=AgriCheck V3
# Leave empty to use same-origin /api (Vite proxy in dev).
# For Hostinger + Railway, set this at build time OR edit public/config.js at runtime.
# VITE_API_BASE_URL=http://localhost:5000
